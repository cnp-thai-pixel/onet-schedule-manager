# คำแนะนำการใช้งาน O-NET Schedule Manager

## 🎯 ภาพรวมระบบ

ระบบจัดการตารางติว O-NET ที่ออกแบบมาให้ใช้งานง่าย ทันสมัย และรองรับทั้ง Admin และผู้ใช้ทั่วไป

### ✨ ฟีเจอร์หลัก

**สำหรับทุกคน (ไม่ต้อง Login):**
- ดูตารางติวทั้งหมด 11 ห้องเรียน
- ดูรายงานการเข้าร่วม
- ดูข้อมูลพื้นฐาน (ห้องเรียน, นักเรียน, ครู, วิชา)
- ส่งออกข้อมูลเป็น CSV

**สำหรับ Admin (ต้อง Login):**
- เพิ่ม/แก้ไข/ลบข้อมูลทั้งหมด
- นำเข้าข้อมูลจาก CSV
- จัดการคาบติวและเช็กชื่อ
- ตั้งค่าระบบ

---

## 🔐 การเข้าสู่ระบบ

### ข้อมูล Login สำหรับทดสอบ:
```
อีเมล: admin@onet.com
รหัสผ่าน: admin123
```

### วิธีการ Login:
1. คลิกปุ่ม "เข้าสู่ระบบ" ที่ Sidebar ด้านล่าง
2. กรอกอีเมลและรหัสผ่าน
3. หรือคลิก "ดูข้อมูลโดยไม่เข้าสู่ระบบ" เพื่อใช้งานในโหมดดูอย่างเดียว

---

## 📋 การใช้งานแต่ละหน้า

### 1. Dashboard (ภาพรวมระบบ)
- แสดง KPI Cards: คาบตามแผน, คาบที่เรียนแล้ว, อัตราเข้าร่วม, นักเรียนเสี่ยง
- แสดงคาบติวในวันนี้
- แสดงข้อมูลสรุป 11 ห้องเรียน

### 2. ตารางติว
- แสดงตารางติวทั้งหมด 11 ห้องเรียน
- แบ่งเป็น 2 Tabs: "วันนี้" และ "ทั้งหมด"
- มีไอคอนรายวิชา: 📘 ไทย, 🔢 คณิต, 🌍 สังคม, 🧬 วิทย์, 🗣️ อังกฤษ
- แสดงสถานะ: OPEN (เปิด), DONE (เสร็จแล้ว), CANCELLED (ยกเลิก)

### 3. คาบวันนี้
- แสดงคาบติวที่มีในวันนี้
- คลิกที่คาบเพื่อไปหน้าเช็กชื่อ

### 4. เช็กชื่อ (Admin เท่านั้น)
- เลือกวิชาและห้องเรียน
- เช็กชื่อนักเรียน 45 คน
- สถานะ: ✅ มา, 🕒 สาย, 📋 ลา, ❌ ขาด
- บันทึกอัตโนมัติ

### 5. รายงาน
- 3 Tabs: รายนักเรียน, รายห้อง, รายวิชา
- แสดง Badge: PASS (ผ่าน), NOT PASS (ไม่ผ่าน)
- ส่งออกเป็น PDF หรือ Excel

### 6. ข้อมูลพื้นฐาน
- จัดการ: ห้องเรียน, นักเรียน, ครูผู้สอน, วิชา
- **ปุ่มสำหรับทุกคน:**
  - 📤 ส่งออก CSV - ดาวน์โหลดข้อมูล
- **ปุ่มสำหรับ Admin:**
  - 📥 นำเข้า CSV - นำเข้าข้อมูลจำนวนมาก
  - ➕ เพิ่ม - เพิ่มข้อมูลทีละรายการ
  - ✏️ แก้ไข - แก้ไขข้อมูล

### 7. ตั้งค่า (Admin เท่านั้น)
- ตั้งค่าระบบทั่วไป
- จัดการผู้ใช้
- ตั้งค่า Firebase

---

## 📥 การนำเข้าข้อมูล CSV

### ขั้นตอน:
1. ไปที่หน้า "ข้อมูลพื้นฐาน"
2. เลือกหมวดที่ต้องการ (ห้องเรียน, นักเรียน, ครู, วิชา)
3. คลิก "📄 ดาวน์โหลดแม่แบบ CSV" เพื่อดูรูปแบบ
4. เตรียมไฟล์ CSV ตามรูปแบบ
5. คลิก "📥 นำเข้า CSV"
6. เลือกไฟล์และตรวจสอบตัวอย่าง
7. คลิก "นำเข้าข้อมูล"

### รูปแบบ CSV:

**นักเรียน (students.csv):**
```csv
studentId,classroom,number,firstName,lastName
12345,ม.6/1,1,สมชาย,ใจดี
12346,ม.6/1,2,สมหญิง,รักดี
```

**ห้องเรียน (classrooms.csv):**
```csv
name,level,studentCount
ม.6/1,ม.6,40
ม.6/2,ม.6,38
```

**ครูผู้สอน (teachers.csv):**
```csv
name,subject,email,phone
ครูสมชาย,คณิต,teacher@example.com,0812345678
```

---

## 🚀 การ Deploy บน GitHub Pages

### ไฟล์ที่ต้องใช้:
- `onet-schedule-manager-github-pages-v2.zip` (ไฟล์ที่ build แล้ว)

### ขั้นตอน:

#### 1. สร้าง GitHub Repository
```bash
# ไปที่ github.com และสร้าง repository ใหม่
# ตั้งชื่อ: onet-schedule-manager
# เลือก Public
```

#### 2. อัปโหลดไฟล์

**วิธีที่ 1: ใช้ GitHub Web Interface**
1. แตกไฟล์ `onet-schedule-manager-github-pages-v2.zip`
2. ลากไฟล์ทั้งหมดไปวางใน repository
3. Commit changes

**วิธีที่ 2: ใช้ Git Command Line**
```bash
# แตกไฟล์
unzip onet-schedule-manager-github-pages-v2.zip -d onet-deploy

# ไปที่โฟลเดอร์
cd onet-deploy

# Initialize git
git init
git add .
git commit -m "Deploy O-NET Schedule Manager"

# เชื่อมต่อกับ GitHub
git remote add origin https://github.com/YOUR_USERNAME/onet-schedule-manager.git
git branch -M main
git push -u origin main
```

#### 3. เปิดใช้งาน GitHub Pages
1. ไปที่ Settings → Pages
2. Source: เลือก `main` branch และ `/ (root)` folder
3. Save
4. รอ 2-3 นาที

#### 4. เข้าถึงเว็บไซต์
```
https://YOUR_USERNAME.github.io/onet-schedule-manager/
```

---

## ⚙️ การตั้งค่า Firebase

### 1. สร้าง Firebase Project
1. ไปที่ [Firebase Console](https://console.firebase.google.com/)
2. สร้างโปรเจ็กต์ใหม่
3. เพิ่ม Web App
4. คัดลอก Firebase Config

### 2. แก้ไขไฟล์ Config
แก้ไขไฟล์ `client/src/lib/firebase.ts`:
```typescript
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
```

### 3. ตั้งค่า Firestore
1. ไปที่ Firestore Database
2. Create Database (เลือก Test mode)
3. ตั้งค่า Security Rules:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // อนุญาตให้อ่านได้ทุกคน
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null; // เฉพาะผู้ที่ login
    }
  }
}
```

### 4. เพิ่มโดเมนที่อนุญาต
1. ไปที่ Authentication → Settings
2. Authorized domains
3. เพิ่ม: `YOUR_USERNAME.github.io`

---

## 🎨 การปรับแต่ง

### เปลี่ยนสีหลัก
แก้ไขไฟล์ `client/src/index.css`:
```css
:root {
  --primary: 215 20% 45%; /* Blue-Gray */
}
```

### เปลี่ยน Logo
แทนที่ URL ใน `client/src/components/DashboardLayout.tsx`:
```tsx
<img src="YOUR_LOGO_URL" alt="Logo" />
```

### เปลี่ยนฟอนต์
แก้ไขใน `client/index.html`:
```html
<link href="https://fonts.googleapis.com/css2?family=YOUR_FONT" rel="stylesheet">
```

---

## 🐛 การแก้ไขปัญหา

### ปัญหา: หน้าเว็บแสดง 404
- ตรวจสอบว่า GitHub Pages ถูกเปิดใช้งาน
- ตรวจสอบว่าไฟล์ `.nojekyll` อยู่ใน repository
- รอ 5-10 นาทีหลัง deploy

### ปัญหา: Firebase ไม่ทำงาน
- ตรวจสอบ Firebase Config
- ตรวจสอบ Security Rules
- ตรวจสอบว่าโดเมนถูกเพิ่มใน Authorized domains

### ปัญหา: ไม่สามารถ Login ได้
- ใช้ข้อมูล: `admin@onet.com` / `admin123`
- ตรวจสอบ localStorage ว่าไม่มีข้อมูลเก่า
- ลองเคลียร์ cache และ reload

### ปัญหา: CSV Import ไม่ทำงาน
- ตรวจสอบรูปแบบ CSV ให้ตรงกับแม่แบบ
- ใช้ UTF-8 encoding
- ตรวจสอบว่าไม่มี comma หรือ quote ผิดรูปแบบ

---

## 📞 การติดต่อและสนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบ Browser Console (F12) เพื่อดู error messages
2. ตรวจสอบ Firebase Console เพื่อดู logs
3. ตรวจสอบ GitHub Actions (ถ้ามี) เพื่อดู deployment status

---

## 📝 หมายเหตุสำคัญ

1. **ข้อมูล Demo:** ระบบใช้ข้อมูลตัวอย่างจาก Firebase Firestore
2. **Authentication:** ใช้ระบบ demo authentication (localStorage)
3. **Production:** ควรเปลี่ยนเป็น Firebase Authentication จริง
4. **Security:** ปรับ Firebase Security Rules ให้เหมาะสมกับการใช้งานจริง
5. **Performance:** พิจารณาใช้ CDN สำหรับ assets ขนาดใหญ่

---

## 🎓 เอกสารเพิ่มเติม

- [Firebase Documentation](https://firebase.google.com/docs)
- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [React Documentation](https://react.dev/)
- [Tailwind CSS Documentation](https://tailwindcss.com/)

---

**เวอร์ชัน:** 2.0  
**อัปเดตล่าสุด:** 5 พฤศจิกายน 2567  
**© 2024 O-NET Schedule Manager**
