# ไฟล์แม่แบบ CSV สำหรับ O-NET Schedule Manager

โฟลเดอร์นี้มีไฟล์แม่แบบ CSV สำหรับนำเข้าข้อมูลเข้าสู่ระบบ

## 📋 ไฟล์แม่แบบที่มี

### 1. students_template.csv (นักเรียน)
**คอลัมน์:**
- `studentId` - รหัสนักเรียน (ตัวเลข 5 หลัก)
- `classroom` - ห้องเรียน (เช่น ม.6/1)
- `number` - เลขที่ในห้อง
- `firstName` - ชื่อ
- `lastName` - นามสกุล

**ตัวอย่าง:**
```csv
studentId,classroom,number,firstName,lastName
67001,ม.6/1,1,สมชาย,ใจดี
67002,ม.6/1,2,สมหญิง,รักดี
```

---

### 2. classrooms_template.csv (ห้องเรียน)
**คอลัมน์:**
- `name` - ชื่อห้อง (เช่น ม.6/1)
- `level` - ระดับชั้น (เช่น ม.6)
- `studentCount` - จำนวนนักเรียน

**ตัวอย่าง:**
```csv
name,level,studentCount
ม.6/1,ม.6,40
ม.6/2,ม.6,38
```

---

### 3. teachers_template.csv (ครูผู้สอน)
**คอลัมน์:**
- `name` - ชื่อครู
- `subject` - วิชาที่สอน
- `email` - อีเมล
- `phone` - เบอร์โทรศัพท์

**ตัวอย่าง:**
```csv
name,subject,email,phone
ครูสมชาย ใจดี,คณิต,somchai@school.ac.th,081-234-5678
ครูสมหญิง รักดี,ไทย,somying@school.ac.th,082-345-6789
```

---

### 4. subjects_template.csv (วิชา)
**คอลัมน์:**
- `name` - ชื่อวิชา
- `code` - รหัสวิชา
- `category` - หมวดวิชา
- `hours` - จำนวนชั่วโมง/สัปดาห์

**ตัวอย่าง:**
```csv
name,code,category,hours
ไทย,TH01,ภาษา,4
คณิต,MA01,คณิตศาสตร์,5
```

---

### 5. sessions_template.csv (คาบติว)
**คอลัมน์:**
- `classroom` - ห้องเรียน
- `subject` - วิชา
- `teacher` - ครูผู้สอน
- `date` - วันที่ (YYYY-MM-DD)
- `day` - วัน (อังคาร, พุธ, ฯลฯ)
- `startTime` - เวลาเริ่ม (HH:MM)
- `endTime` - เวลาสิ้นสุด (HH:MM)
- `status` - สถานะ (OPEN, DONE, CANCELLED)

**ตัวอย่าง:**
```csv
classroom,subject,teacher,date,day,startTime,endTime,status
ม.6/1,คณิต,ครูสมชาย ใจดี,2024-11-05,อังคาร,08:30,10:20,OPEN
ม.6/2,ไทย,ครูสมหญิง รักดี,2024-11-05,อังคาร,10:30,12:20,OPEN
```

---

## 🔧 วิธีการใช้งาน

### 1. ดาวน์โหลดแม่แบบ
1. เปิดระบบ O-NET Schedule Manager
2. ไปที่หน้า "ข้อมูลพื้นฐาน"
3. เลือกหมวดที่ต้องการ
4. คลิก "📄 ดาวน์โหลดแม่แบบ CSV"

### 2. แก้ไขข้อมูล
1. เปิดไฟล์ CSV ด้วย Excel, Google Sheets หรือ Text Editor
2. แก้ไขข้อมูลตามต้องการ
3. **สำคัญ:** บันทึกเป็น CSV (UTF-8) เท่านั้น

### 3. นำเข้าข้อมูล
1. ไปที่หน้า "ข้อมูลพื้นฐาน"
2. เลือกหมวดที่ต้องการ
3. คลิก "📥 นำเข้า CSV"
4. เลือกไฟล์ที่แก้ไขแล้ว
5. ตรวจสอบตัวอย่างข้อมูล
6. คลิก "นำเข้าข้อมูล"

---

## ⚠️ ข้อควรระวัง

### การใช้ Comma (,)
- ถ้าข้อมูลมี comma ให้ใส่เครื่องหมาย quotes
- ตัวอย่าง: `"สมชาย, ใจดี"`

### การใช้ Quotes (")
- ถ้าข้อมูลมี quotes ให้ใช้ double quotes
- ตัวอย่าง: `"ครู ""สมชาย"" ใจดี"`

### Encoding
- **ต้องใช้ UTF-8 เท่านั้น** เพื่อรองรับภาษาไทย
- Excel: Save As → CSV UTF-8
- Google Sheets: Download → CSV

### รูปแบบวันที่
- ใช้รูปแบบ `YYYY-MM-DD` เท่านั้น
- ตัวอย่าง: `2024-11-05`
- **ห้าม** ใช้ `05/11/2024` หรือ `5 พ.ย. 2567`

### รูปแบบเวลา
- ใช้รูปแบบ `HH:MM` (24 ชั่วโมง)
- ตัวอย่าง: `08:30`, `13:00`, `16:50`
- **ห้าม** ใช้ `8:30 AM` หรือ `1:00 PM`

---

## 💡 เทคนิคการใช้งาน

### การนำเข้าข้อมูลจำนวนมาก
1. เตรียมข้อมูลใน Excel/Google Sheets
2. ใช้ Formula ช่วยสร้างข้อมูล
3. Export เป็น CSV UTF-8
4. นำเข้าทีละไฟล์

### การอัปเดตข้อมูล
1. ส่งออกข้อมูลปัจจุบัน
2. แก้ไขในไฟล์ที่ส่งออก
3. นำเข้ากลับเข้าระบบ

### การสำรองข้อมูล
1. ส่งออกข้อมูลทุกหมวดเป็น CSV
2. เก็บไฟล์ไว้เป็นสำรอง
3. สามารถนำเข้ากลับได้ทุกเมื่อ

---

## 🎯 ตัวอย่างการใช้งาน

### สร้างนักเรียน 40 คนในห้อง ม.6/1
```csv
studentId,classroom,number,firstName,lastName
67001,ม.6/1,1,นักเรียน1,นามสกุล1
67002,ม.6/1,2,นักเรียน2,นามสกุล2
...
67040,ม.6/1,40,นักเรียน40,นามสกุล40
```

### สร้างตารางติว 1 สัปดาห์
```csv
classroom,subject,teacher,date,day,startTime,endTime,status
ม.6/1,คณิต,ครูสมชาย,2024-11-05,อังคาร,08:30,10:20,OPEN
ม.6/1,ไทย,ครูสมหญิง,2024-11-05,อังคาร,10:30,12:20,OPEN
ม.6/1,อังกฤษ,ครูวิชัย,2024-11-06,พุธ,08:30,10:20,OPEN
...
```

---

## 📞 ต้องการความช่วยเหลือ?

หากพบปัญหาในการนำเข้าข้อมูล:
1. ตรวจสอบรูปแบบไฟล์ให้ตรงกับแม่แบบ
2. ตรวจสอบ encoding เป็น UTF-8
3. ตรวจสอบว่าไม่มี comma หรือ quote ผิดรูปแบบ
4. ลองนำเข้าข้อมูลทีละไฟล์แทนการนำเข้าทั้งหมดพร้อมกัน

---

**เวอร์ชัน:** 2.0  
**อัปเดตล่าสุด:** 5 พฤศจิกายน 2567
