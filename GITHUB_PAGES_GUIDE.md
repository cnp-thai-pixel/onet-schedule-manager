# คู่มือการใช้งาน O-NET Schedule Manager บน GitHub Pages

## 📋 สารบัญ
1. [การเตรียมไฟล์](#การเตรียมไฟล์)
2. [การ Deploy บน GitHub Pages](#การ-deploy-บน-github-pages)
3. [การใช้งานระบบ](#การใช้งานระบบ)
4. [ข้อมูลสำหรับทดสอบ](#ข้อมูลสำหรับทดสอบ)
5. [การนำเข้าข้อมูล CSV](#การนำเข้าข้อมูล-csv)
6. [คำถามที่พบบ่อย](#คำถามที่พบบ่อย)

---

## 📦 การเตรียมไฟล์

ไฟล์ที่คุณได้รับ: `onet-schedule-manager-github-pages-final.zip`

### โครงสร้างไฟล์ภายใน:
```
onet-schedule-manager-github-pages-final/
├── index.html                      # หน้าเว็บหลัก
├── assets/                         # ไฟล์ CSS และ JavaScript
│   ├── index-JRCrPWAW.css
│   └── index-BK7Fblcn.js
├── csv-templates/                  # ไฟล์แม่แบบ CSV
│   ├── students_template.csv
│   ├── classrooms_template.csv
│   ├── teachers_template.csv
│   ├── subjects_template.csv
│   ├── sessions_template.csv
│   └── README.md
├── DEMO_CREDENTIALS.md             # ข้อมูลสำหรับทดสอบ
└── .nojekyll                       # ไฟล์สำหรับ GitHub Pages
```

---

## 🚀 การ Deploy บน GitHub Pages

### ขั้นตอนที่ 1: สร้าง Repository ใหม่บน GitHub

1. เข้าสู่ GitHub (https://github.com)
2. คลิกปุ่ม **"New"** หรือ **"+"** ที่มุมขวาบน
3. เลือก **"New repository"**
4. ตั้งชื่อ Repository เช่น `onet-schedule-manager`
5. เลือก **Public** (สำคัญ: ต้องเป็น Public เพื่อใช้ GitHub Pages ฟรี)
6. **ไม่ต้อง** เลือก "Initialize this repository with a README"
7. คลิก **"Create repository"**

### ขั้นตอนที่ 2: แตกไฟล์ ZIP

1. แตกไฟล์ `onet-schedule-manager-github-pages-final.zip`
2. จะได้โฟลเดอร์ที่มีไฟล์ทั้งหมดข้างในรวมถึง `index.html`

### ขั้นตอนที่ 3: Upload ไฟล์ขึ้น GitHub

#### วิธีที่ 1: ใช้ GitHub Web Interface (ง่ายที่สุด)

1. เข้าไปที่ Repository ที่สร้างไว้
2. คลิก **"uploading an existing file"** หรือ **"Add file" → "Upload files"**
3. ลากไฟล์ทั้งหมดจากโฟลเดอร์ที่แตกไว้มาวางในหน้าเว็บ
   - **สำคัญ**: ต้อง Upload ทั้ง `index.html`, โฟลเดอร์ `assets`, `csv-templates`, `DEMO_CREDENTIALS.md` และ `.nojekyll`
4. เขียน Commit message เช่น "Initial commit"
5. คลิก **"Commit changes"**

#### วิธีที่ 2: ใช้ Git Command Line

```bash
# 1. เปิด Terminal/Command Prompt ในโฟลเดอร์ที่แตกไฟล์
cd path/to/extracted/folder

# 2. Initialize Git
git init

# 3. เพิ่มไฟล์ทั้งหมด
git add .

# 4. Commit
git commit -m "Initial commit"

# 5. เชื่อมต่อกับ GitHub (แทน YOUR_USERNAME และ YOUR_REPO)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# 6. Push ขึ้น GitHub
git branch -M main
git push -u origin main
```

### ขั้นตอนที่ 4: เปิดใช้งาน GitHub Pages

1. ใน Repository ของคุณ คลิกที่ **"Settings"** (เมนูด้านบน)
2. เลื่อนลงหรือคลิกเมนู **"Pages"** ทางซ้ายมือ
3. ในส่วน **"Source"**:
   - เลือก Branch: **main**
   - เลือก Folder: **/ (root)**
4. คลิก **"Save"**
5. รอสักครู่ (1-2 นาที) แล้ว Refresh หน้า
6. จะเห็นข้อความ **"Your site is live at https://YOUR_USERNAME.github.io/YOUR_REPO/"**

### ขั้นตอนที่ 5: ทดสอบเว็บไซต์

1. คลิกที่ URL ที่ได้ เช่น `https://YOUR_USERNAME.github.io/onet-schedule-manager/`
2. หน้าเว็บควรโหลดขึ้นมาพร้อม Landing Page สีน้ำเงินสวยงาม

---

## 💻 การใช้งานระบบ

### หน้า Landing Page

เมื่อเปิดเว็บไซต์ครั้งแรก คุณจะเห็น:

1. **Hero Section** - ข้อมูลภาพรวมระบบ
   - 80 คาบติว
   - 440 นักเรียน
   - 11 ห้องเรียน
   - 85% อัตราเข้าร่วม

2. **ปุ่มเข้าสู่ระบบ** - คลิกเพื่อไปหน้า Login
3. **ปุ่มเรียนรู้เพิ่มเติม** - เลื่อนลงดูฟีเจอร์

4. **ส่วน Features**:
   - จัดการตารางติว
   - เช็กชื่ออัตโนมัติ
   - รายงานและวิเคราะห์
   - ระบบสิทธิ์ 3 ระดับ

5. **Role Selection Cards**:
   - 👨‍🎓 **นักเรียน** - ดูข้อมูลอย่างเดียว
   - 👨‍🏫 **ครูผู้สอน** - เช็กชื่อและอัปเดตข้อมูลตัวเอง
   - 🛡️ **ผู้ดูแลระบบ** - ทำได้ทุกอย่าง

### การเข้าสู่ระบบ

1. คลิกปุ่ม **"เข้าสู่ระบบ"** หรือคลิกที่ Role Card
2. เลือก Role ที่ต้องการ (นักเรียน / ครู / ผู้ดูแลระบบ)
3. กรอกอีเมลและรหัสผ่าน (ดูข้อมูลทดสอบด้านล่าง)
4. คลิก **"เข้าสู่ระบบ"**

---

## 🔐 ข้อมูลสำหรับทดสอบ

### 👨‍🎓 นักเรียน (Student)

**สิทธิ์:**
- ดูตารางติวของตัวเอง
- ดูประวัติการเข้าเรียน
- ดูคะแนนเข้าร่วม
- **ไม่สามารถ**แก้ไขข้อมูลใด ๆ

**ข้อมูลเข้าสู่ระบบ:**
```
อีเมล: student@onet.com
รหัสผ่าน: student123
```

**เมนูที่เห็น:**
- 🏠 Dashboard
- 📅 ตารางติว
- 📊 ประวัติการเข้าเรียน

---

### 👨‍🏫 ครูผู้สอน (Teacher)

**สิทธิ์:**
- ดูตารางติวที่ตัวเองสอน
- **เช็กชื่อ**นักเรียนในคาบที่สอน
- อัปเดตข้อมูลส่วนตัว
- ดูรายงานการเข้าเรียนของนักเรียน

**ข้อมูลเข้าสู่ระบบ:**
```
อีเมล: teacher@onet.com
รหัสผ่าน: teacher123
```

**เมนูที่เห็น:**
- 🏠 Dashboard
- 📅 ตารางติว
- ✅ เช็กชื่อ
- 📊 รายงาน

---

### 🛡️ ผู้ดูแลระบบ (Admin)

**สิทธิ์:**
- เข้าถึงได้**ทุกหน้า**
- จัดการข้อมูลทั้งหมด (เพิ่ม แก้ไข ลบ)
- ดู Dashboard แบบเต็ม
- จัดการผู้ใช้ทั้งหมด
- ส่งออกรายงาน CSV
- นำเข้าข้อมูล CSV

**ข้อมูลเข้าสู่ระบบ:**
```
อีเมล: admin@onet.com
รหัสผ่าน: admin123
```

**เมนูที่เห็น:**
- 🏠 Dashboard (พร้อมกราฟและสถิติครบถ้วน)
- 📅 ตารางติว
- ✅ เช็กชื่อ
- 📊 รายงาน
- 🗂️ จัดการข้อมูล (Masters)
- ⚙️ ตั้งค่า

---

## 📊 การนำเข้าข้อมูล CSV

### ไฟล์แม่แบบ CSV ที่มีให้

ในโฟลเดอร์ `csv-templates` มีไฟล์แม่แบบ 5 ไฟล์:

1. **students_template.csv** - ข้อมูลนักเรียน
2. **classrooms_template.csv** - ข้อมูลห้องเรียน
3. **teachers_template.csv** - ข้อมูลครูผู้สอน
4. **subjects_template.csv** - ข้อมูลวิชา
5. **sessions_template.csv** - ข้อมูลคาบติว

### วิธีการนำเข้าข้อมูล

1. **ดาวน์โหลดไฟล์แม่แบบ**:
   - เข้าไปที่ `https://YOUR_USERNAME.github.io/YOUR_REPO/csv-templates/`
   - ดาวน์โหลดไฟล์ที่ต้องการ

2. **แก้ไขข้อมูล**:
   - เปิดไฟล์ด้วย Excel, Google Sheets หรือ Text Editor
   - แก้ไขข้อมูลตามต้องการ
   - **สำคัญ**: ห้ามเปลี่ยนชื่อคอลัมน์ (Header Row)

3. **บันทึกเป็น CSV**:
   - File → Save As → เลือก **CSV (Comma delimited)**
   - ตั้งชื่อไฟล์ตามต้องการ

4. **นำเข้าข้อมูล**:
   - Login เป็น **Admin**
   - ไปที่หน้า **"จัดการข้อมูล"** (Masters)
   - เลือกแท็บที่ต้องการ (นักเรียน / ห้องเรียน / ครู / วิชา / คาบติว)
   - คลิกปุ่ม **"📥 นำเข้าไฟล์ CSV"**
   - เลือกไฟล์ CSV ที่แก้ไขแล้ว
   - ตรวจสอบข้อมูลที่แสดง
   - คลิก **"นำเข้า"**

### ตัวอย่างข้อมูลในไฟล์ CSV

**students_template.csv:**
```csv
id,name,classroom,email,phone
S001,สมชาย ใจดี,ม.6/1,somchai@example.com,081-234-5678
S002,สมหญิง รักเรียน,ม.6/1,somying@example.com,082-345-6789
```

**sessions_template.csv:**
```csv
id,subject,classroom,teacher,date,time,status
SE001,คณิตศาสตร์,ม.6/1,ครูสมชาย,2024-11-05,08:30-10:20,OPEN
SE002,ภาษาไทย,ม.6/2,ครูสมหญิง,2024-11-05,10:30-12:20,OPEN
```

---

## 📱 การใช้งานบนมือถือ

ระบบรองรับการใช้งานบนมือถือ (Responsive Design):

1. เปิดเว็บไซต์บนมือถือ
2. เมนูจะแสดงเป็นแบบ Hamburger Menu (☰)
3. คลิกที่ไอคอน ☰ เพื่อเปิด/ปิดเมนู
4. ฟีเจอร์ทั้งหมดใช้งานได้เหมือนบนคอมพิวเตอร์

---

## ❓ คำถามที่พบบ่อย

### Q1: เว็บไซต์ไม่แสดงผล หรือแสดง 404 Not Found

**A:** ตรวจสอบดังนี้:
1. ตรวจสอบว่า Branch ใน Settings → Pages เป็น `main` และ Folder เป็น `/` (root)
2. ตรวจสอบว่ามีไฟล์ `index.html` ใน root ของ repository
3. รอ 1-2 นาทีหลังจาก Deploy ครั้งแรก
4. ลอง Clear Cache ของเบราว์เซอร์ (Ctrl+Shift+R หรือ Cmd+Shift+R)

### Q2: ข้อมูลที่แก้ไขหายไปเมื่อ Refresh หน้า

**A:** ระบบนี้ใช้ Firebase Firestore เป็น Database ซึ่งต้องตั้งค่า Firebase Project ก่อน ในเวอร์ชันปัจจุบัน ข้อมูลจะเก็บใน Local Storage ของเบราว์เซอร์ ถ้าต้องการให้ข้อมูลคงอยู่ถาวร ต้องตั้งค่า Firebase (ดูคำแนะนำใน README.md)

### Q3: ต้องการเปลี่ยน URL เป็นชื่อโดเมนของตัวเอง

**A:** 
1. ซื้อโดเมนจาก Domain Registrar (เช่น Namecheap, GoDaddy)
2. ใน GitHub Repository → Settings → Pages → Custom domain
3. กรอกชื่อโดเมนของคุณ เช่น `onet.example.com`
4. ตั้งค่า DNS ที่ Domain Registrar:
   - เพิ่ม CNAME record ที่ชี้ไปที่ `YOUR_USERNAME.github.io`
5. รอ DNS Propagation (อาจใช้เวลา 24-48 ชั่วโมง)

### Q4: ต้องการแก้ไขสีหรือโลโก้

**A:** ต้องแก้ไขโค้ดต้นฉบับ:
1. ดาวน์โหลด Source Code จาก Manus
2. แก้ไขไฟล์ `client/src/index.css` (สำหรับสี)
3. แก้ไขไฟล์ `client/index.html` (สำหรับโลโก้)
4. Build ใหม่ด้วยคำสั่ง `pnpm build`
5. Upload ไฟล์ใน `dist/public` ขึ้น GitHub อีกครั้ง

### Q5: ต้องการเพิ่ม/ลดจำนวนห้องเรียนหรือวิชา

**A:** 
1. Login เป็น Admin
2. ไปที่หน้า "จัดการข้อมูล" (Masters)
3. เลือกแท็บ "ห้องเรียน" หรือ "วิชา"
4. คลิกปุ่ม "+ เพิ่มห้องเรียน" หรือ "+ เพิ่มวิชา"
5. กรอกข้อมูลและบันทึก

หรือนำเข้าผ่าน CSV (ดูวิธีการด้านบน)

---

## 🔧 การอัปเดตเว็บไซต์

หากต้องการอัปเดตเว็บไซต์:

1. แก้ไขไฟล์ในโฟลเดอร์ต้นฉบับ
2. Upload ไฟล์ใหม่ขึ้น GitHub (แทนที่ไฟล์เดิม)
3. รอ 1-2 นาที
4. Refresh หน้าเว็บ (อาจต้อง Clear Cache)

---

## 📞 การติดต่อและสนับสนุน

หากมีปัญหาหรือข้อสงสัย:
- อ่านไฟล์ `README.md` ใน Repository
- ตรวจสอบ `DEMO_CREDENTIALS.md` สำหรับข้อมูลทดสอบ
- ดู `csv-templates/README.md` สำหรับวิธีการใช้ CSV

---

## ✅ Checklist การ Deploy

- [ ] สร้าง GitHub Repository แล้ว
- [ ] แตกไฟล์ ZIP แล้ว
- [ ] Upload ไฟล์ทั้งหมดขึ้น GitHub แล้ว
- [ ] เปิดใช้งาน GitHub Pages แล้ว (Settings → Pages)
- [ ] ทดสอบเปิดเว็บไซต์แล้ว
- [ ] ทดสอบ Login ด้วย Admin แล้ว
- [ ] ดาวน์โหลด CSV Templates แล้ว
- [ ] อ่านคู่มือการใช้งานแล้ว

---

**🎉 ขอให้ใช้งานระบบจัดการตารางติว O-NET อย่างมีความสุขค่ะ!**
